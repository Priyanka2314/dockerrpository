sudo docker pull <image>
sudo docker images
##drawback in the command if we exit then container got exited state and will not running state.
sudo docker run -it --name <containername> -p 80:80 <image>
sudo docker ps
sudo docker ps -a
sudo docker rm -f $(sudo docker ps -a -q)  => delete all containers in one go
sudo docker run -it -d --name <containername> -p 82:80 <image> => container has been created but not logged in.

##logged in container
sudo docker exec -it <containername> bash

sudo docker commit <container-id> <dockhubrepo>/<imagename> 

#push the imag
-> first login to Docker hub
sudo docker login
->then push the img
sudo docker push <dockhubrepo>/img

##pull the img
sudo docker pull <dockhubrepo>/<imagename>
#create or build img
sudo  docker build . -t <giveanyimgname>
#delete image
sudo docker rmi <imgname>
